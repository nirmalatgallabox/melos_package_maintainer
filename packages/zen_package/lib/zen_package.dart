library zen_package;

import 'package:flutter/material.dart';

/// A Calculator.
class ZenPackage extends StatelessWidget {
  const ZenPackage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Zen Package v0.0.2');
  }
}
