library hello_package;

import 'package:flutter/widgets.dart';

class HelloPackage extends StatelessWidget {
  const HelloPackage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Hello Package v0.0.1');
  }
}
